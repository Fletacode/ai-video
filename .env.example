# ElevenLabs (v3 TTS)
# 로컬 slide-narrator/.env 가 없으면 ../connect-ppt/.env → ../redub/.env 순으로 fallback 한다.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
# v3 대본 포맷은 eleven_v3 를 전제로 설계됨 (오디오 태그가 텍스트로 해석됨)
ELEVENLABS_MODEL_ID=eleven_v3
# 세그먼트/청크 간 톤 일관성을 위한 고정 seed (선택, 기본 42)
ELEVENLABS_SEED=42
